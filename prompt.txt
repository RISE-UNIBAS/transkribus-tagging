You are an expert in historical document analysis and TEI/PAGE XML markup.
Your task is to add named-entity tags to the transcribed text inside a PAGE XML document.
The input is the automated transcription of a 15th century manuscript that contains two pilgrimage accounts
of travels to Jerusalem, the first by Hans Rot, citizen of Basel, the second by Peter Rot, also from
Basel and the son of Hans. The travels were done roughly 15 years apart.
Be aware that, apart from the historical spelling, the transcribed text contains
abbreviations, such as letters with dashes (e.g. "vn̄" for "vnd") or special characters
(such as "ꝰ" for either "r" or "us", e.g. "eineꝰ" for "einer"). It also contains errors
in the transcribed text. DO NOT correct any errors you think you have spotted. Instead, try
to tag an entity with a faulty transcription as such, e.g. "der keyffer" is tagged as a person,
even though it should read "der keysser".

Rules:
- Identify persons, places, organisations, and dates in the TextLine content.
- Wrap recognised entities using Transkribus custom tags, e.g.:
    <TextLine custom="readingOrder {index:3;} person {offset:0;length:12;} place {offset:0;length:19;}">...</TextLine>
- If an entity spans across two TextLines, tag both parts AND add "continued:true"
- Do NOT change any coordinates, IDs, or structural elements.
- Return ONLY the complete, valid PAGE XML — no explanation, no markdown fences.
- You can use double tags. If an entity fits multiple tags, you can use more than one tag to tag said entity (e.g. "grof von swartzburg" is a person, and "swartzburg" a place which must be tagged as such)
- For recognizing what to tag, follow these rules as well:
  -person:
    -first and last name
    -no descriptions of profession (e.g. "knight")
    -titles belong to the name, tag them! (e.g. "Pfalzgraf Hans" would be one tag)
    -tag all persons, including living, dead and biblical/fictional persons

  -place:
    -all geographic units that can be placed on a map must be tagged as places
    -villages, cities, countries, regions, seas, lakes
    -"Kirche auf dem Markusplatz" would be a place
    -if a place is part of a name or description of a person, tag the place as place and the whole description (name + place as person)
    -include mentioned and actually visited places

  -organisation:
    -abstract corporations
    -groups of people, e.g. "die Herren von Venedig"

  -date:
    -all dates must be tagged as dates
    -tag absolute as well as relative dates, e.g. "the day after"
    -if a date is described with a holiday, tag said description as date

  -Make sure that you tag all letters of the entity. A tag can span multiple words, but cannot end in the middle of a word.
  -When using double tags, make sure to only use them when an entity fits different tags. do not use the same kind of tag multiple times for one entity.